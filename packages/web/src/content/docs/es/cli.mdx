---
title: CLI
description: Opciones y comandos de la CLI de shob.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI de shob de forma predeterminada inicia el [TUI](/docs/tui) cuando se ejecuta sin ningún argumento.

```bash
shob
```

Pero también acepta comandos como se documenta en esta página. Esto le permite interactuar con shob mediante programación.

```bash
shob run "Explain how closures work in JavaScript"
```

---

### tui

Inicie la interfaz de usuario del terminal shob.

```bash
shob [project]
```

#### Opciones

| Opción       | Corta | Descripción                                                           |
| ------------ | ----- | --------------------------------------------------------------------- |
| `--continue` | `-c`  | Continuar la última sesión                                            |
| `--session`  | `-s`  | ID de sesión para continuar                                           |
| `--fork`     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`) |
| `--prompt`   |       | Aviso de uso                                                          |
| `--model`    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                        |
| `--agent`    |       | Agente a utilizar                                                     |
| `--port`     |       | Puerto para escuchar                                                  |
| `--hostname` |       | Nombre de host para escuchar                                          |

---

## Comandos

La CLI de shob también tiene los siguientes comandos.

---

### agent

Administrar agentes para shob.

```bash
shob agent [command]
```

---

### attach

Conecte una terminal a un servidor backend shob que ya se esté ejecutando y iniciado mediante los comandos `serve` o `web`.

```bash
shob attach [url]
```

Esto permite usar TUI con un backend remoto shob. Por ejemplo:

```bash
# Start the backend server for web/mobile access
shob web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
shob attach http://10.20.30.40:4096
```

#### Opciones

| Opción      | Corta | Descripción                               |
| ----------- | ----- | ----------------------------------------- |
| `--dir`     |       | Directorio de trabajo para iniciar TUI en |
| `--session` | `-s`  | ID de sesión para continuar               |

---

#### create

Cree un nuevo agente con configuración personalizada.

```bash
shob agent create
```

Este comando lo guiará en la creación de un nuevo agente con un mensaje del sistema personalizado y una configuración de herramientas.

---

#### list

Enumere todos los agentes disponibles.

```bash
shob agent list
```

---

### auth

Comando para administrar credenciales e iniciar sesión para proveedores.

```bash
shob auth [command]
```

---

#### login

shob funciona con la lista de proveedores en [Models.dev](https://models.dev), por lo que puede usar `shob auth login` para configurar las claves API para cualquier proveedor que desee utilizar. Esto se almacena en `~/.local/share/shob/auth.json`.

```bash
shob auth login
```

Cuando se inicia shob, carga los proveedores desde el archivo de credenciales. Y si hay claves definidas en sus entornos o un archivo `.env` en su proyecto.

---

#### list

Enumera todos los proveedores autenticados tal como están almacenados en el archivo de credenciales.

```bash
shob auth list
```

O la versión corta.

```bash
shob auth ls
```

---

#### logout

Cierra tu sesión de un proveedor eliminándolo del archivo de credenciales.

```bash
shob auth logout
```

---

### github

Administre el agente GitHub para la automatización del repositorio.

```bash
shob github [command]
```

---

#### install

Instale el agente GitHub en su repositorio.

```bash
shob github install
```

Esto configura el flujo de trabajo de acciones GitHub necesario y lo guía a través del proceso de configuración. [Más información](/docs/github).

---

#### run

Ejecute el agente GitHub. Esto se usa normalmente en acciones GitHub.

```bash
shob github run
```

##### Opciones

| Opción    | Descripción                                    |
| --------- | ---------------------------------------------- |
| `--event` | GitHub evento simulado para ejecutar el agente |
| `--token` | GitHub token de acceso personal                |

---

### mcp

Administrar servidores de protocolo de contexto modelo.

```bash
shob mcp [command]
```

---

#### add

Agregue un servidor MCP a su configuración.

```bash
shob mcp add
```

Este comando lo guiará para agregar un servidor MCP local o remoto.

---

#### list

Enumere todos los servidores MCP configurados y su estado de conexión.

```bash
shob mcp list
```

O utilice la versión corta.

```bash
shob mcp ls
```

---

#### auth

Autentíquese con un servidor MCP habilitado para OAuth.

```bash
shob mcp auth [name]
```

Si no proporciona un nombre de servidor, se le pedirá que seleccione entre los servidores compatibles con OAuth disponibles.

También puede enumerar los servidores compatibles con OAuth y su estado de autenticación.

```bash
shob mcp auth list
```

O utilice la versión corta.

```bash
shob mcp auth ls
```

---

#### logout

Elimine las credenciales OAuth para un servidor MCP.

```bash
shob mcp logout [name]
```

---

#### debug

Depurar problemas de conexión OAuth para un servidor MCP.

```bash
shob mcp debug <name>
```

---

### models

Enumere todos los modelos disponibles de los proveedores configurados.

```bash
shob models [provider]
```

Este comando muestra todos los modelos disponibles en sus proveedores configurados en el formato `provider/model`.

Esto es útil para determinar el nombre exacto del modelo que se usará en [su configuración](/docs/config/).

Opcionalmente, puede pasar un ID de proveedor para filtrar modelos por ese proveedor.

```bash
shob models anthropic
```

#### Opciones

| Opción      | Descripción                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--refresh` | Actualizar la caché de modelos desde models.dev                             |
| `--verbose` | Utilice una salida del modelo más detallada (incluye metadatos como costos) |

Utilice el indicador `--refresh` para actualizar la lista de modelos almacenados en caché. Esto es útil cuando se han agregado nuevos modelos a un proveedor y desea verlos en shob.

```bash
shob models --refresh
```

---

### run

Ejecute shob en modo no interactivo pasando un mensaje directamente.

```bash
shob run [message..]
```

Esto es útil para secuencias de comandos, automatización o cuando desea una respuesta rápida sin iniciar el TUI completo. Por ejemplo.

```bash "shob run"
shob run Explain the use of context in Go
```

También puede conectarse a una instancia `shob serve` en ejecución para evitar tiempos de arranque en frío del servidor MCP en cada ejecución:

```bash
# Start a headless server in one terminal
shob serve

# In another terminal, run commands that attach to it
shob run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opciones

| Opción       | Corta | Descripción                                                                         |
| ------------ | ----- | ----------------------------------------------------------------------------------- |
| `--command`  |       | El comando a ejecutar, use mensaje para args                                        |
| `--continue` | `-c`  | Continuar la última sesión                                                          |
| `--session`  | `-s`  | ID de sesión para continuar                                                         |
| `--fork`     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`)               |
| `--share`    |       | Comparte la sesión                                                                  |
| `--model`    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                                      |
| `--agent`    |       | Agente a utilizar                                                                   |
| `--file`     | `-f`  | Archivo(s) para adjuntar al mensaje                                                 |
| `--format`   |       | Formato: predeterminado (formateado) o json (eventos JSON sin formato)              |
| `--title`    |       | Título de la sesión (utiliza un mensaje truncado si no se proporciona ningún valor) |
| `--attach`   |       | Adjuntar a un servidor shob en ejecución (por ejemplo, http://localhost:4096)   |
| `--port`     |       | Puerto para el servidor local (el puerto predeterminado es aleatorio)               |

---

### serve

Inicie un servidor shob sin cabeza para acceso API. Consulte los [documentos del servidor](/docs/server) para conocer la interfaz HTTP completa.

```bash
shob serve
```

Esto inicia un servidor HTTP que proporciona acceso API a la funcionalidad shob sin la interfaz TUI. Configure `shob_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `shob`).

#### Opciones

| Opción       | Descripción                                          |
| ------------ | ---------------------------------------------------- |
| `--port`     | Puerto para escuchar                                 |
| `--hostname` | Nombre de host para escuchar                         |
| `--mdns`     | Habilitar el descubrimiento de mDNS                  |
| `--cors`     | Orígenes de navegador adicionales para permitir CORS |

---

### session

Administrar shob sesiones.

```bash
shob session [command]
```

---

#### list

Enumere todas las sesiones shob.

```bash
shob session list
```

##### Opciones

| Opción        | Corta | Descripción                             |
| ------------- | ----- | --------------------------------------- |
| `--max-count` | `-n`  | Limitar a N sesiones más recientes      |
| `--format`    |       | Formato de salida: tabla o json (tabla) |

---

### stats

Muestre el uso de tokens y las estadísticas de costos para sus sesiones shob.

```bash
shob stats
```

#### Opciones

| Opción      | Descripción                                                                                                              |
| ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| `--days`    | Mostrar estadísticas de los últimos N días (todo el tiempo)                                                              |
| `--tools`   | Número de herramientas para mostrar (todas)                                                                              |
| `--models`  | Mostrar el desglose del uso del modelo (oculto de forma predeterminada). Pase un número para mostrar la parte superior N |
| `--project` | Filtrar por proyecto (todos los proyectos, cadena vacía: proyecto actual)                                                |

---

### export

Exportar datos de la sesión como JSON.

```bash
shob export [sessionID]
```

Si no proporciona una ID de sesión, se le pedirá que seleccione entre las sesiones disponibles.

---

### import

Importe datos de sesión desde un archivo JSON o una URL compartida shob.

```bash
shob import <file>
```

Puede importar desde un archivo local o una URL compartida shob.

```bash
shob import session.json
shob import https://opncd.ai/s/abc123
```

---

### web

Inicie un servidor shob sin cabeza con una interfaz web.

```bash
shob web
```

Esto inicia un servidor HTTP y abre un navegador web para acceder a shob a través de una interfaz web. Configure `shob_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `shob`).

#### Opciones

| Opción       | Descripción                                          |
| ------------ | ---------------------------------------------------- |
| `--port`     | Puerto para escuchar                                 |
| `--hostname` | Nombre de host para escuchar                         |
| `--mdns`     | Habilitar el descubrimiento de mDNS                  |
| `--cors`     | Orígenes de navegador adicionales para permitir CORS |

---

### acp

Inicie un servidor ACP (Agent Client Protocol).

```bash
shob acp
```

Este comando inicia un servidor ACP que se comunica a través de stdin/stdout usando nd-JSON.

#### Opciones

| Opción       | Descripción                  |
| ------------ | ---------------------------- |
| `--cwd`      | Directorio de trabajo        |
| `--port`     | Puerto para escuchar         |
| `--hostname` | Nombre de host para escuchar |

---

### uninstall

Desinstale shob y elimine todos los archivos relacionados.

```bash
shob uninstall
```

#### Opciones

| Opción          | Corta | Descripción                               |
| --------------- | ----- | ----------------------------------------- |
| `--keep-config` | `-c`  | Mantener archivos de configuración        |
| `--keep-data`   | `-d`  | Conservar datos de sesión e instantáneas  |
| `--dry-run`     |       | Mostrar lo que se eliminaría sin eliminar |
| `--force`       | `-f`  | Saltar mensajes de confirmación           |

---

### upgrade

Actualiza shob a la última versión o a una versión específica.

```bash
shob upgrade [target]
```

Para actualizar a la última versión.

```bash
shob upgrade
```

Para actualizar a una versión específica.

```bash
shob upgrade v0.1.48
```

#### Opciones

| Opción     | Corta | Descripción                                                         |
| ---------- | ----- | ------------------------------------------------------------------- |
| `--method` | `-m`  | El método de instalación que se utilizó; curl, npm, pnpm, bun, brew |

---

## Opciones globales

La CLI de shob toma las siguientes banderas globales.

| Opción         | Corta | Descripción                                  |
| -------------- | ----- | -------------------------------------------- |
| `--help`       | `-h`  | Mostrar ayuda                                |
| `--version`    | `-v`  | Número de versión de impresión               |
| `--print-logs` |       | Imprimir registros en stderr                 |
| `--log-level`  |       | Nivel de registro (DEBUG, INFO, WARN, ERROR) |

---

## Variables de entorno

shob se puede configurar mediante variables de entorno.

| Variable                              | Type     | Description                                                                     |
| ------------------------------------- | -------- | ------------------------------------------------------------------------------- |
| `shob_AUTO_SHARE`                 | booleano | Compartir sesiones automáticamente                                              |
| `shob_GIT_BASH_PATH`              | cadena   | Ruta al ejecutable de Git Bash en Windows                                       |
| `shob_CONFIG`                     | cadena   | Ruta al archivo de configuración                                                |
| `shob_CONFIG_DIR`                 | cadena   | Ruta al directorio de configuración                                             |
| `shob_CONFIG_CONTENT`             | cadena   | Contenido de configuración json en línea                                        |
| `shob_DISABLE_AUTOUPDATE`         | booleano | Deshabilitar las comprobaciones automáticas de actualizaciones                  |
| `shob_DISABLE_PRUNE`              | booleano | Deshabilitar la poda de datos antiguos                                          |
| `shob_DISABLE_TERMINAL_TITLE`     | booleano | Deshabilitar las actualizaciones automáticas de títulos de terminal             |
| `shob_PERMISSION`                 | cadena   | Configuración de permisos json incorporados                                     |
| `shob_DISABLE_DEFAULT_PLUGINS`    | booleano | Deshabilitar complementos predeterminados                                       |
| `shob_DISABLE_LSP_DOWNLOAD`       | booleano | Deshabilitar las descargas automáticas del servidor LSP                         |
| `shob_ENABLE_EXPERIMENTAL_MODELS` | booleano | Habilitar modelos experimentales                                                |
| `shob_DISABLE_AUTOCOMPACT`        | booleano | Deshabilitar la compactación automática de contexto                             |
| `shob_DISABLE_CLAUDE_CODE`        | booleano | Deshabilitar la lectura desde `.claude` (mensaje + habilidades)                 |
| `shob_DISABLE_CLAUDE_CODE_PROMPT` | booleano | Desactivar lectura `~/.claude/CLAUDE.md`                                        |
| `shob_DISABLE_CLAUDE_CODE_SKILLS` | booleano | Deshabilitar la carga `.claude/skills`                                          |
| `shob_DISABLE_MODELS_FETCH`       | booleano | Deshabilitar la recuperación de modelos desde fuentes remotas                   |
| `shob_FAKE_VCS`                   | cadena   | Proveedor de VCS falso para fines de prueba                                     |
| `shob_DISABLE_FILETIME_CHECK`     | booleano | Deshabilite la verificación del tiempo del archivo para optimizarlo             |
| `shob_CLIENT`                     | cadena   | Identificador de cliente (por defecto `cli`)                                    |
| `shob_ENABLE_EXA`                 | booleano | Habilitar las herramientas de búsqueda web de Exa                               |
| `shob_SERVER_PASSWORD`            | cadena   | Habilite la autenticación básica para `serve`/`web`                             |
| `shob_SERVER_USERNAME`            | cadena   | Anular el nombre de usuario de autenticación básica (predeterminado `shob`) |
| `shob_MODELS_URL`                 | cadena   | URL personalizada para buscar la configuración de modelos                       |

---

### Experimental

Estas variables de entorno habilitan funciones experimentales que pueden cambiar o eliminarse.

| Variable                                        | Type     | Description                                                |
| ----------------------------------------------- | -------- | ---------------------------------------------------------- |
| `shob_EXPERIMENTAL`                         | booleano | Habilitar todas las funciones experimentales               |
| `shob_EXPERIMENTAL_ICON_DISCOVERY`          | booleano | Habilitar descubrimiento de íconos                         |
| `shob_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | booleano | Deshabilitar copia al seleccionar en TUI                   |
| `shob_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | número   | Tiempo de espera predeterminado para comandos bash en ms   |
| `shob_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | número   | Tokens de salida máximos para respuestas LLM               |
| `shob_EXPERIMENTAL_FILEWATCHER`             | booleano | Habilite el observador de archivos para todo el directorio |
| `shob_EXPERIMENTAL_OXFMT`                   | booleano | Habilitar el formateador oxfmt                             |
| `shob_EXPERIMENTAL_LSP_TOOL`                | booleano | Habilitar herramienta experimental LSP                     |
| `shob_EXPERIMENTAL_DISABLE_FILEWATCHER`     | booleano | Deshabilitar el observador de archivos                     |
| `shob_EXPERIMENTAL_EXA`                     | booleano | Habilitar funciones experimentales de Exa                  |
| `shob_EXPERIMENTAL_LSP_TY`                  | booleano | Habilitar Habilitar TY LSP para archivos python            |
| `shob_EXPERIMENTAL_MARKDOWN`                | booleano | Habilitar funciones de Markdown experimentales             |
| `shob_EXPERIMENTAL_PLAN_MODE`               | booleano | Habilitar modo de plan                                     |
