---
title: CLI
description: Opções e comandos da CLI do shob.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

A CLI do shob, por padrão, inicia o [TUI](/docs/tui) quando executada sem argumentos.

```bash
shob
```

Mas também aceita comandos conforme documentado nesta página. Isso permite que você interaja com o shob programaticamente.

```bash
shob run "Explain how closures work in JavaScript"
```

---

### tui

Inicie a interface de usuário do terminal do shob.

```bash
shob [project]
```

#### Opções

| Flag         | Curto | Descrição                                                                  |
| ------------ | ----- | -------------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue a última sessão                                                   |
| `--session`  | `-s`  | ID da sessão para continuar                                                |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`) |
| `--prompt`   |       | Prompt a ser usado                                                         |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                              |
| `--agent`    |       | Agente a ser usado                                                         |
| `--port`     |       | Porta para escutar                                                         |
| `--hostname` |       | Nome do host para escutar                                                  |

---

## Comandos

A CLI do shob também possui os seguintes comandos.

---

### agent

Gerencie agentes para o shob.

```bash
shob agent [command]
```

---

### attach

Anexe um terminal a um servidor backend do shob já em execução, iniciado via comandos `serve` ou `web`.

```bash
shob attach [url]
```

Isso permite usar o TUI com um backend shob remoto. Por exemplo:

```bash
# Inicie o servidor backend para acesso web/mobile
shob web --port 4096 --hostname 0.0.0.0

# Em outro terminal, anexe o TUI ao backend em execução
shob attach http://10.20.30.40:4096
```

#### Opções

| Flag        | Curto | Descrição                                |
| ----------- | ----- | ---------------------------------------- |
| `--dir`     |       | Diretório de trabalho para iniciar o TUI |
| `--session` | `-s`  | ID da sessão para continuar              |

---

#### create

Crie um novo agente com configuração personalizada.

```bash
shob agent create
```

Este comando irá guiá-lo na criação de um novo agente com um prompt de sistema personalizado e configuração de ferramentas.

---

#### list

Liste todos os agentes disponíveis.

```bash
shob agent list
```

---

### auth

Comando para gerenciar credenciais e login para provedores.

```bash
shob auth [command]
```

---

#### login

O shob é alimentado pela lista de provedores em [Models.dev](https://models.dev), então você pode usar `shob auth login` para configurar chaves de API para qualquer provedor que você gostaria de usar. Isso é armazenado em `~/.local/share/shob/auth.json`.

```bash
shob auth login
```

Quando o shob é iniciado, ele carrega os provedores do arquivo de credenciais. E se houver chaves definidas em seus ambientes ou em um arquivo `.env` em seu projeto.

---

#### list

Lista todos os provedores autenticados conforme armazenado no arquivo de credenciais.

```bash
shob auth list
```

Ou a versão curta.

```bash
shob auth ls
```

---

#### logout

Desconecta você de um provedor limpando-o do arquivo de credenciais.

```bash
shob auth logout
```

---

### github

Gerencie o agente do GitHub para automação de repositórios.

```bash
shob github [command]
```

---

#### install

Instale o agente do GitHub em seu repositório.

```bash
shob github install
```

Isso configura o fluxo de trabalho necessário do GitHub Actions e o guia pelo processo de configuração. [Saiba mais](/docs/github).

---

#### run

Execute o agente do GitHub. Isso é tipicamente usado em GitHub Actions.

```bash
shob github run
```

##### Opções

| Flag      | Descrição                                        |
| --------- | ------------------------------------------------ |
| `--event` | Evento simulado do GitHub para executar o agente |
| `--token` | Token de acesso pessoal do GitHub                |

---

### mcp

Gerencie servidores do Model Context Protocol.

```bash
shob mcp [command]
```

---

#### add

Adicione um servidor MCP à sua configuração.

```bash
shob mcp add
```

Este comando irá guiá-lo na adição de um servidor MCP local ou remoto.

---

#### list

Liste todos os servidores MCP configurados e seu status de conexão.

```bash
shob mcp list
```

Ou use a versão curta.

```bash
shob mcp ls
```

---

#### auth

Autentique-se com um servidor MCP habilitado para OAuth.

```bash
shob mcp auth [name]
```

Se você não fornecer um nome de servidor, será solicitado que você selecione entre os servidores disponíveis habilitados para OAuth.

Você também pode listar servidores habilitados para OAuth e seu status de autenticação.

```bash
shob mcp auth list
```

Ou use a versão curta.

```bash
shob mcp auth ls
```

---

#### logout

Remova credenciais OAuth para um servidor MCP.

```bash
shob mcp logout [name]
```

---

#### debug

Depure problemas de conexão OAuth para um servidor MCP.

```bash
shob mcp debug <name>
```

---

### models

Liste todos os modelos disponíveis dos provedores configurados.

```bash
shob models [provider]
```

Este comando exibe todos os modelos disponíveis entre seus provedores configurados no formato `provider/model`.

Isso é útil para descobrir o nome exato do modelo a ser usado em [sua configuração](/docs/config/).

Você pode opcionalmente passar um ID de provedor para filtrar modelos por esse provedor.

```bash
shob models anthropic
```

#### Opções

| Flag        | Descrição                                                             |
| ----------- | --------------------------------------------------------------------- |
| `--refresh` | Atualiza o cache de modelos a partir do models.dev                    |
| `--verbose` | Use uma saída de modelo mais detalhada (inclui metadados como custos) |

Use a flag `--refresh` para atualizar a lista de modelos em cache. Isso é útil quando novos modelos foram adicionados a um provedor e você deseja vê-los no shob.

```bash
shob models --refresh
```

---

### run

Execute o shob em modo não interativo passando um prompt diretamente.

```bash
shob run [message..]
```

Isso é útil para scripts, automação ou quando você deseja uma resposta rápida sem iniciar o TUI completo. Por exemplo.

```bash "shob run"
shob run Explique o uso de context em Go
```

Você também pode se anexar a uma instância em execução do `shob serve` para evitar tempos de inicialização a frio do servidor MCP em cada execução:

```bash
# Inicie um servidor sem cabeça em um terminal
shob serve

# Em outro terminal, execute comandos que se anexam a ele
shob run --attach http://localhost:4096 "Explique async/await em JavaScript"
```

#### Opções

| Flag         | Curto | Descrição                                                                     |
| ------------ | ----- | ----------------------------------------------------------------------------- |
| `--command`  |       | O comando a ser executado, use mensagem para argumentos                       |
| `--continue` | `-c`  | Continue a última sessão                                                      |
| `--session`  | `-s`  | ID da sessão para continuar                                                   |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`)    |
| `--share`    |       | Compartilhe a sessão                                                          |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                                 |
| `--agent`    |       | Agente a ser usado                                                            |
| `--file`     | `-f`  | Arquivo(s) a serem anexados à mensagem                                        |
| `--format`   |       | Formato: padrão (formatado) ou json (eventos JSON brutos)                     |
| `--title`    |       | Título para a sessão (usa o prompt truncado se nenhum valor for fornecido)    |
| `--attach`   |       | Anexe a um servidor shob em execução (por exemplo, http://localhost:4096) |
| `--port`     |       | Porta para o servidor local (padrão para porta aleatória)                     |

---

### serve

Inicie um servidor shob sem cabeça para acesso à API. Confira a [documentação do servidor](/docs/server) para a interface HTTP completa.

```bash
shob serve
```

Isso inicia um servidor HTTP que fornece acesso à funcionalidade do shob sem a interface TUI. Defina `shob_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `shob`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### session

Gerencie sessões do shob.

```bash
shob session [command]
```

---

#### list

Liste todas as sessões do shob.

```bash
shob session list
```

##### Opções

| Flag          | Curto | Descrição                                 |
| ------------- | ----- | ----------------------------------------- |
| `--max-count` | `-n`  | Limitar às N sessões mais recentes        |
| `--format`    |       | Formato de saída: tabela ou json (tabela) |

---

### stats

Mostre o uso de tokens e estatísticas de custo para suas sessões do shob.

```bash
shob stats
```

#### Opções

| Flag        | Descrição                                                                                            |
| ----------- | ---------------------------------------------------------------------------------------------------- |
| `--days`    | Mostre estatísticas dos últimos N dias (todo o tempo)                                                |
| `--tools`   | Número de ferramentas a serem mostradas (todas)                                                      |
| `--models`  | Mostre a divisão do uso de modelos (oculto por padrão). Passe um número para mostrar os N principais |
| `--project` | Filtrar por projeto (todos os projetos, string vazia: projeto atual)                                 |

---

### export

Exporte dados da sessão como JSON.

```bash
shob export [sessionID]
```

Se você não fornecer um ID de sessão, será solicitado que você selecione entre as sessões disponíveis.

---

### import

Importe dados da sessão de um arquivo JSON ou URL de compartilhamento do shob.

```bash
shob import <file>
```

Você pode importar de um arquivo local ou de uma URL de compartilhamento do shob.

```bash
shob import session.json
shob import https://opncd.ai/s/abc123
```

---

### web

Inicie um servidor shob sem cabeça com uma interface web.

```bash
shob web
```

Isso inicia um servidor HTTP e abre um navegador para acessar o shob através de uma interface web. Defina `shob_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `shob`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### acp

Inicie um servidor ACP (Agent Client Protocol).

```bash
shob acp
```

Este comando inicia um servidor ACP que se comunica via stdin/stdout usando nd-JSON.

#### Opções

| Flag         | Descrição                 |
| ------------ | ------------------------- |
| `--cwd`      | Diretório de trabalho     |
| `--port`     | Porta para escutar        |
| `--hostname` | Nome do host para escutar |

---

### uninstall

Desinstale o shob e remova todos os arquivos relacionados.

```bash
shob uninstall
```

#### Opções

| Flag            | Curto | Descrição                                |
| --------------- | ----- | ---------------------------------------- |
| `--keep-config` | `-c`  | Manter arquivos de configuração          |
| `--keep-data`   | `-d`  | Manter dados de sessão e snapshots       |
| `--dry-run`     |       | Mostrar o que seria removido sem remover |
| `--force`       | `-f`  | Pular prompts de confirmação             |

---

### upgrade

Atualiza o shob para a versão mais recente ou uma versão específica.

```bash
shob upgrade [target]
```

Para atualizar para a versão mais recente.

```bash
shob upgrade
```

Para atualizar para uma versão específica.

```bash
shob upgrade v0.1.48
```

#### Opções

| Flag       | Curto | Descrição                                                        |
| ---------- | ----- | ---------------------------------------------------------------- |
| `--method` | `-m`  | O método de instalação que foi usado; curl, npm, pnpm, bun, brew |

---

## Opções Globais

A CLI do shob aceita as seguintes flags globais.

| Flag           | Curto | Descrição                               |
| -------------- | ----- | --------------------------------------- |
| `--help`       | `-h`  | Exibir ajuda                            |
| `--version`    | `-v`  | Imprimir número da versão               |
| `--print-logs` |       | Imprimir logs no stderr                 |
| `--log-level`  |       | Nível de log (DEBUG, INFO, WARN, ERROR) |

---

## Variáveis de ambiente

O shob pode ser configurado usando variáveis de ambiente.

| Variável                              | Tipo    | Descrição                                                             |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `shob_AUTO_SHARE`                 | boolean | Compartilhar sessões automaticamente                                  |
| `shob_GIT_BASH_PATH`              | string  | Caminho para o executável do Git Bash no Windows                      |
| `shob_CONFIG`                     | string  | Caminho para o arquivo de configuração                                |
| `shob_CONFIG_DIR`                 | string  | Caminho para o diretório de configuração                              |
| `shob_CONFIG_CONTENT`             | string  | Conteúdo de configuração json inline                                  |
| `shob_DISABLE_AUTOUPDATE`         | boolean | Desabilitar verificações de atualização automática                    |
| `shob_DISABLE_PRUNE`              | boolean | Desabilitar a poda de dados antigos                                   |
| `shob_DISABLE_TERMINAL_TITLE`     | boolean | Desabilitar atualizações automáticas do título do terminal            |
| `shob_PERMISSION`                 | string  | Configuração de permissões json inline                                |
| `shob_DISABLE_DEFAULT_PLUGINS`    | boolean | Desabilitar plugins padrão                                            |
| `shob_DISABLE_LSP_DOWNLOAD`       | boolean | Desabilitar downloads automáticos do servidor LSP                     |
| `shob_ENABLE_EXPERIMENTAL_MODELS` | boolean | Habilitar modelos experimentais                                       |
| `shob_DISABLE_AUTOCOMPACT`        | boolean | Desabilitar compactação automática de contexto                        |
| `shob_DISABLE_CLAUDE_CODE`        | boolean | Desabilitar leitura de `.claude` (prompt + habilidades)               |
| `shob_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Desabilitar leitura de `~/.claude/CLAUDE.md`                          |
| `shob_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Desabilitar carregamento de `.claude/skills`                          |
| `shob_DISABLE_MODELS_FETCH`       | boolean | Desabilitar busca de modelos de fontes remotas                        |
| `shob_FAKE_VCS`                   | string  | Provedor VCS falso para fins de teste                                 |
| `shob_DISABLE_FILETIME_CHECK`     | boolean | Desabilitar verificação de tempo de arquivo para otimização           |
| `shob_CLIENT`                     | string  | Identificador do cliente (padrão é `cli`)                             |
| `shob_ENABLE_EXA`                 | boolean | Habilitar ferramentas de busca web Exa                                |
| `shob_SERVER_PASSWORD`            | string  | Habilitar autenticação básica para `serve`/`web`                      |
| `shob_SERVER_USERNAME`            | string  | Substituir nome de usuário de autenticação básica (padrão `shob`) |
| `shob_MODELS_URL`                 | string  | URL personalizada para buscar configuração de modelos                 |

---

### Experimental

Essas variáveis de ambiente habilitam recursos experimentais que podem mudar ou ser removidos.

| Variável                                        | Tipo    | Descrição                                                 |
| ----------------------------------------------- | ------- | --------------------------------------------------------- |
| `shob_EXPERIMENTAL`                         | boolean | Habilitar todos os recursos experimentais                 |
| `shob_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Habilitar descoberta de ícones                            |
| `shob_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Desabilitar cópia ao selecionar no TUI                    |
| `shob_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Tempo limite padrão para comandos bash em ms              |
| `shob_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Máximo de tokens de saída para respostas LLM              |
| `shob_EXPERIMENTAL_FILEWATCHER`             | boolean | Habilitar monitoramento de arquivos para todo o diretório |
| `shob_EXPERIMENTAL_OXFMT`                   | boolean | Habilitar formatador oxfmt                                |
| `shob_EXPERIMENTAL_LSP_TOOL`                | boolean | Habilitar ferramenta LSP experimental                     |
| `shob_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Desabilitar monitoramento de arquivos                     |
| `shob_EXPERIMENTAL_EXA`                     | boolean | Habilitar recursos experimentais do Exa                   |
| `shob_EXPERIMENTAL_LSP_TY`                  | boolean | Habilitar TY LSP para arquivos python                     |
| `shob_EXPERIMENTAL_MARKDOWN`                | boolean | Habilitar recursos experimentais de markdown              |
| `shob_EXPERIMENTAL_PLAN_MODE`               | boolean | Habilitar modo de plano                                   |
